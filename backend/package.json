{
  "name": "bluto-backend",
  "version": "0.1.0",
  "description": "Install dependencies with `npm install`",
  "main": "api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
